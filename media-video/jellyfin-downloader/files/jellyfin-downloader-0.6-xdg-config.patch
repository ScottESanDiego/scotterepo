Use a user-writable XDG config path while retaining the explicit
JELLYDOWN_CONFIG_FILE override. Create the destination directory before
saving, and report the release version shipped by this tag.

--- a/jellydown/config.py
+++ b/jellydown/config.py
@@ -4,7 +4,9 @@ import json
 import os
 from pathlib import Path
 
-DEFAULT_CONFIG_FILE = Path(__file__).parent.parent / "jellydown.json"
+DEFAULT_CONFIG_FILE = Path(
+    os.environ.get("XDG_CONFIG_HOME", Path.home() / ".config")
+) / "jellydown" / "jellydown.json"
 
 
 def config_path() -> Path:
@@ -48,7 +50,9 @@ def load_config():
 
 def save_config(cfg: dict):
     """Save configuration to file."""
-    config_path().write_text(
+    path = config_path()
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
         json.dumps(cfg, indent=2),
         encoding="utf-8",
     )
--- a/jellydown/__init__.py
+++ b/jellydown/__init__.py
@@ -1,3 +1,3 @@
 """JellyfinDownloader - Download movies and TV series from Jellyfin."""
 
-__version__ = "1.0.0"
+__version__ = "0.6"
